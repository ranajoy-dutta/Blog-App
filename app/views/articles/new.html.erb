<h1>New Article</h1>
<br><br>

<%= form_with scope: :toform, url: articles_path, local: true do |form| %>
<%#= form_for @toform, url: articles_path do |form| %>
  
  <% if @toform.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@toform.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @toform.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>
 
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
 
  <p>
    <%= form.submit 'Save Article' %>
  </p>
<% end %>

<%= link_to 'Back', root_path %>